procedure base b expansion(n, b: integer and b > 1)
    q := n
    k := 0
    while q != 0
        a[k] := q mod b
        q := q div b
        k := k + 1
    return binary a[] { a[k - 1]...a[1], a[0] }
